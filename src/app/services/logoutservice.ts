import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class LogoutService {
  constructor() {}

  // Simulate a logout process
  logout(): void {
    // Your logout logic here, such as clearing user session or tokens
  }
}
